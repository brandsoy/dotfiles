-- Hyprland Lua configuration (Hyprland >= 0.55)
-- https://wiki.hypr.land/Configuring/
-- Split into modules matching the former configuration sections.

require("monitors")
require("environment")
require("autostart")
require("appearance")
require("keybindings")
require("window_rules")
