# Atuin complements the existing zsh substring-history bindings.
# Search remains local-only; enable sync deliberately if wanted later.
auto_sync = false
search_mode = "fuzzy"
filter_mode = "directory"
style = "compact"
inline_height = 20
show_preview = true
enter_accept = true
secrets_filter = true
