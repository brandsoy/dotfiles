# Dotfiles readme

## Init

To symlink all configs to ~/dotfiles/
run in terminal

```bash
stow -v -t ~ */
```

# Clean old install - Linux / MacOS (unix)
rm -rf ~/.config/nvim
rm -rf ~/.local/state/nvim
rm -rf ~/.local/share/nvim
